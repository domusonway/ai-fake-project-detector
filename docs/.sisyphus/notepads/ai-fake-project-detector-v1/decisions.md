## Notepad: Decisions

- 2026-04-01: Persistence foundation uses a local SQLite store (`services/persistence.py`) with append-only `analysis_snapshots`, repo identity normalization, latest-snapshot ranking queries, and time-window feedback dedupe so downstream ranking/detail/feedback work can reuse stable contracts without recomputing core scores.
- 2026-04-01: Persistence test cleanup keeps behavior unchanged while replacing bare `dict` annotations with typed shapes/casts; the remaining `services.persistence` import-style warning is tolerated because the project root is not a normal importable package path.
- 2026-04-01: App-surface ownership is now explicit: `flask_app.py` is the canonical V1 web + JSON API runtime, shared analysis orchestration moved into `app_runtime.py`, `streamlit_app.py` reuses that flow as a demo/dev companion only, and `simple_app.py` remains mock-only on a non-primary port with explicit demo notices.
- 2026-04-01: `app_runtime.py` typing cleanup stayed surgical: replaced bare `dict` annotations with typed mapping/dict forms to remove avoidable local diagnostics without changing runtime behavior or app-surface ownership.
- 2026-04-01: Product-language normalization is now part of the V1 contract: user-facing docs use verification / credibility / risk wording instead of accusatory framing, keep GitHub as the sole MVP source, scope rankings to analyzed repos only, and define authenticated lightweight feedback as supplemental rather than score-authoritative.
- 2026-04-01: Primary analysis orchestration now runs repo ingestion → structure analysis → rule-first peer retrieval → risk scoring → append-only snapshot persistence through a single `analyze_repository(...)` contract, using the local SQLite store for stable `/api/analyze` and `/api/reanalyze` snapshot metadata/history semantics.
- 2026-04-01: T8 diagnostics cleanup stayed local to orchestration files: invalid TypedDict casts in the orchestration test now cast through `object`, and file-local pyright ignores suppress repo-layout implicit-import noise without changing runtime imports or orchestration behavior.
- 2026-04-01: Repo detail/history now reads persisted snapshots from `PersistenceStore.get_repo_history(...)` to show the latest report plus chronological prior snapshots, and the detail UI keeps sparse/low-confidence peer disclaimers fully visible instead of recomputing or hiding guardrail context.
- 2026-04-01: Ranking/search/filter now stays on `PersistenceStore.query_rankings(...)` for analyzed-repo-only results, exposes transparent sort/filter query state over current snapshot fields (search, language, risk band, sort, direction, limit/offset), and labels any feedback counts as supplemental rather than rank-authoritative.
- 2026-04-01: V1 feedback auth/governance stays intentionally lightweight: a local session-backed username gates `support` / `disagree` / `needs_review` submissions, persistence dedupe is reused for the 60-minute collapse window, a per-repo cooldown adds basic anti-abuse rate limiting, and UI/API only expose masked supplemental summaries/activity so feedback never becomes score- or rank-authoritative.
- 2026-04-01: Main-surface integration now uses Flask route/navigation helpers plus the shared `_report_sections.html` macros as the canonical source for score cards, repo metadata, peer disclaimers, evidence cards, and guardrail sections; `/api/analyze` now exposes the same persisted-detail/rankings navigation contract that the landing/report/detail/rankings flow uses in the UI.
- 2026-04-01: Repo detail now normalizes persisted snapshots into a compatibility shape before rendering so real flattened history rows from `analyze_repository(...)` still satisfy the detail template's `scoring_result` access pattern without changing scoring semantics.
- 2026-04-01: Final V1 doc sync now treats Flask as the only canonical shipped surface, documents persisted orchestration/detail/history/rankings/authenticated supplemental feedback as already shipped behavior, and moves remaining work into an honest post-V1 backlog instead of stale pre-implementation TODOs.
- 2026-04-01: Final-wave blocker closeout was administrative only: added `.sisyphus/evidence/` F1-F4 artifacts from already-performed QA facts (`53 passed`, analyze/reanalyze snapshots 1->2, detail/rankings live success, feedback 401/auth success/429/dedupe, Chart.js `bar` + `indexAxis: 'y'`) without changing runtime behavior.
- 2026-04-01: Added post-delivery handoff docs (`README.md`, `docs/DELIVERY_SUMMARY.md`) to make the shipped Flask startup path, analyzed-repo-only rankings boundary, and authenticated supplemental feedback boundary explicit without overstating deferred scope.
